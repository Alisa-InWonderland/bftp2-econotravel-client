import React from "react";
import  { TopNavBar } from './TopNavBar'

export const Header = () => {
    return (
        <header className="header">
            <TopNavBar/>
        </header>
    );
};