import React from "react";
import { BottomNavBar } from "./BottomNavBar";

export const Footer = () => {
    return (
        <footer className="footer">
            <BottomNavBar/>
        </footer>
    );
};